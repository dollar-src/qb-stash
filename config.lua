Config = {}



Config.Locations = {
    {
        type = 2, -- marker type / https://docs.fivem.net/docs/game-references/markers/
        name = "GUNSHOP STASH", -- stash name must be unique
        coord = vector3(21.45, -1105.03, 29.8), -- stash coord
        job = 'police', -- stash job
        MarkerColor = {
            R = 255, -- Marker Color (R)
            G = 0,   -- Marker Color (G)
            B = 0,   -- Marker Color (B)
            A = 70,  -- Marker Color (A)
        },
        MarkerSize = {
            X = 0.5,  -- Marker Size (X)
            Y = 0.5,  -- Marker Size (Y)
            Z = 0.5,  -- Marker Size (Z)
        },
        Menu = {
            Header = 'Gun Shop Stash', -- Menu Header Text
            Info = 'Good place to hide weapons', -- Menu Secound Text
        }
    },
    {
        type = 2, -- marker type / https://docs.fivem.net/docs/game-references/markers/
        name = "GUNSHOP STASH", -- stash name must be unique
        coord = vector3(21.45, -1105.03, 29.8), -- stash coord
        job = 'ambulance', -- stash job
        MarkerColor = {
            R = 255, -- Marker Color (R)
            G = 0,   -- Marker Color (G)
            B = 0,   -- Marker Color (B)
            A = 70,  -- Marker Color (A)
        },
        MarkerSize = {
            X = 0.5,  -- Marker Size (X)
            Y = 0.5,  -- Marker Size (Y)
            Z = 0.5,  -- Marker Size (Z)
        },
        Menu = {
            Header = 'Gun Shop Stash', -- Menu Header Text
            Info = 'Good place to hide weapons', -- Menu Secound Text
        }
    },

}

